# Display real-time parking availability
```
cd ./client

#if node is NOT installed and Linux/Ubuntu environment:
#install nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash
source ~/.bashrc
#install node v24
nvm install 24 

#build dependencies if your first time
npm install

#run page
npm run dev
```
